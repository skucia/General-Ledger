# Marks "app/services" as a Python package. Business logic (DB queries, validation, transaction posting) will live
# here so the route handlers stay thin and a future React frontend can call the same services.
