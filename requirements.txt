# Tested on Python 3.9 (local dev on the Mac) and Python 3.12 (the
# python:3.12-slim base used by the Dockerfile). Newer point releases of
# anything below should be fine; bump deliberately when needed.

# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.6

# Templates and form/file handling for FastAPI
jinja2==3.1.4
python-multipart==0.0.9

# Postgres driver (v3) with the C extension for speed
psycopg[binary]==3.1.18

# Password hashing
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Config loader
python-dotenv==1.0.1

# Session middleware (used in Phase 2)
itsdangerous==2.2.0
